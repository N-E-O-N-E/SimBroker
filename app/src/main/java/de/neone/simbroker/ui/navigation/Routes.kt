package de.neone.simbroker.ui.navigation

/**
 * Enthält alle Routen-Keys für die Navigation innerhalb der App.
 *
 * Jede Konstante repräsentiert den Namen einer Screen-Route,
 * die beim Navigieren verwendet wird.
 */
object Routes {

    //==============================================================================================
    // 1) Hauptbereiche
    //==============================================================================================

    /** Route für das Portfolio-Screen. */
    const val PORTFOLIO = "portfolio"

    /** Route für das Coins-Screen. */
    const val COINS = "coins"

    /** Route für das Account-Screen. */
    const val ACCOUNT = "account"

    //==============================================================================================
    // 2) Sonstige
    //==============================================================================================

    /** Route für den Logout-Vorgang. */
    const val LOGOUT = "logout"
}
